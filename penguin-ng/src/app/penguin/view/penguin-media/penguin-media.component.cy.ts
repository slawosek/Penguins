import { PenguinMediaSelectionComponent } from './penguin-media-selection.component'

describe('PenguinMediaComponent', () => {
  it('should mount', () => {
    cy.mount(PenguinMediaSelectionComponent)
  })
})
